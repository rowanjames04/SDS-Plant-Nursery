"""Stripe payment integration module."""

# This module is intentionally minimal to avoid circular imports.
# The stripe.api_key is set in app.py after configuration loads.
# Import functions from submodules directly where needed.
